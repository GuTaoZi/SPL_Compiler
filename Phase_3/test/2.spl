int fib(int n)
{
    int a[100];
    int i;
    a[0] = a[1] = 1;
    for (i = 2; i <= n; i = i + 1)
    {
        a[i] = a[i - 1] + a[i - 2];
        write(a[i]);
    }
    write(a[n]);
}

int main()
{
    int n = read();
    fib(n);
}